#include <iostream>
using namespace std;
int summ(int a, int b)
{
	return a + b;
}
int i_forgor_the_word(int a, int b)
{
	return a * b;
}
int main()
{
	summ(10, 10);
	//i must have dementia because i genuenly don't remember how to do them and study materials or whatever tf it's called aren't helping
	i_forgor_the_word(10, 20);
	return 0;
}